/**
 *
 */
import { Response, NextFunction } from 'express';
import AuthService from '../api/services/auth/auth-service';

export default async function addUser(req: any, res: Response, next: NextFunction) {

  next();
}
